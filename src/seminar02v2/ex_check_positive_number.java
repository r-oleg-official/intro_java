package seminar02v2;

import java.util.Scanner;

public class ex_check_positive_number {
    public static void main(String[] args) {
        int size = 0;
        System.out.print("Enter a even positive number: ");
        Scanner scn = new Scanner(System.in);
        String regex = "^[1-9]\\d*$";
        //while (!input.matches(regex) || Integer.parseInt(input) % 2 != 0)
        System.out.println(size);
    }
}
